import { ApolloProvider } from '@elitevalet/network/src/config/apollo'
import '@elitevalet/ui/src/app/globals.css'
import { Container } from '@elitevalet/ui/src/components/atoms/Container'
import { SessionProvider } from '@elitevalet/ui/src/components/molecules/SessionProvider'
import { ToastContainer } from '@elitevalet/ui/src/components/molecules/Toast'
import { Header } from '@elitevalet/ui/src/components/organisms/Header'
import { MenuItem } from '@elitevalet/util/types'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'EliteValet',
  description: 'Generated by create next app',
}

const MENUITEMS: MenuItem[] = [
  { label: 'Search', href: '/search' },
  { label: 'Bookings', href: '/bookings' },
]

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <SessionProvider>
        <ApolloProvider>
          <body className={`${inter.className} bg-gray-25`}>
            <Header menuItems={MENUITEMS} />
            <Container>{children}</Container>
            <ToastContainer />
          </body>
        </ApolloProvider>
      </SessionProvider>
    </html>
  )
}
